# SpringBootAngularBasic


This project is combination of spring boot application (backend) and angular (frontend) (any version can be used) 

# How to run project

mvn clean package 
 
cd ./backend/target 
 
java -jar backend-0.0.1-SNAPSHOT.jar 
